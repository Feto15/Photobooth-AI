{
    "name": "photobooth-ai",
    "version": "1.0.0",
    "private": true,
    "description": "Photobooth AI Monorepo",
    "packageManager": "pnpm@9.15.5",
    "scripts": {
        "dev": "pnpm --parallel dev",
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "lint": "pnpm -r lint"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "prettier": "^3.0.0"
    }
}
